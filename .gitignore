# Dependencies
node_modules/

# Build output
dist/
storybook-static/

# Design-sync working state (staged scripts, generated bundle, machine caches)
.ds-sync/
ds-bundle/
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules

# App data (SQLite db + uploads) — single source of truth, never committed
data/

# Logs & misc
*.log
.DS_Store
.env
.env.local
