# dependencies
node_modules/

# build output
dist/

# next.js app (app/)
app/.next/
app/node_modules/
app/next-env.d.ts
app/tsconfig.tsbuildinfo

# design-sync (converter scratch + machine state; durable config/notes ARE committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# data (SQLite db + uploads — see CLAUDE.md; volume in prod)
data/

# local env / secrets (SITE_PASSWORD, AUTH_SECRET, ...)
.env*.local

# impeccable local machine state (hook cache, local config)
.impeccable/

# spike generated artifacts (keep the source: build.mjs, package.json)
spike/**/build.bundle.mjs
spike/**/index.html
spike/**/*.png

# misc
.DS_Store
*.log
